// const inputs = document.querySelectorAll('.input-file');
// Array.prototype.forEach.call(inputs, (input) => {
//   const label = input.nextElementSibling,
//     labelVal = label.querySelector('.your-sample__dounload').innerText;
//   input.addEventListener('change', function (e) {
//     let countFiles = '';
//     if (this.files && this.files.length >= 1) {
//       countFiles = this.files.length;
//     }

//     if (countFiles) {
//       label.querySelector('.your-sample__dounload').innerText = `Выбрано файлов: ${ countFiles}`;
//     } else {
//       label.querySelector('.your-sample__dounload').innerText = labelVal;
//     }
//   });
// });
